# requirements.txt
# Enfocado en Linux (Omarchy) por ahora — dlib no tiene el lío de
# instalación que tiene en Windows (eso se retoma aparte cuando
# volvamos a empaquetar para Windows).

flask
pandas
numpy
opencv-python
face_recognition
pyserial
pywebview[qt]
pyinstaller
