# requirements-windows.txt
# Para la PC Windows de la escuela (deployment final).
#
# pywebview en Windows usa Edge WebView2 (pythonnet + clr) de forma
# automática al instalar en esta plataforma - no hace falta el extra
# [qt] ni [gtk] que sí hace falta en Linux.
#
# Requisito del sistema (no de pip): Windows 10/11 ya trae WebView2
# Runtime instalado por default. Si la PC es más vieja o viene "pelada",
# bajarlo de https://developer.microsoft.com/microsoft-edge/webview2/

flask
pandas
numpy
opencv-python
pyserial
pywebview
pyinstaller
