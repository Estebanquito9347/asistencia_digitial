# requirements-linux.txt
# Para desarrollar/probar en Omarchy (o cualquier Linux).
#
# pywebview necesita un motor gráfico embebido en Linux (no viene solo,
# a diferencia de Windows que usa el WebView2 del sistema). El extra
# [qt] trae PyQt5 + PyQtWebEngine, 100% instalable con pip, sin tocar
# pacman para nada de Python. Es la opción más simple en Arch/Omarchy.

flask
pandas
numpy
opencv-contrib-python
pyserial
pywebview[qt]
pyinstaller
